import React from 'react';
import logo from './../logo.svg';

const Header = () => (
  <header className="App-header">
    <div>
      <img src={logo} className="App-logo" alt="logo" />
      <span>DOWNstream for Electron - Redux Integration Example</span>
    </div>
  </header>
)

export default Header;
